# Receiver_Module — ARM Cortex Firmware Makefile
# Auto-generated by Hardware Pipeline v2

CROSS_COMPILE ?= arm-none-eabi-
CC      = $(CROSS_COMPILE)gcc
OBJCOPY = $(CROSS_COMPILE)objcopy
SIZE    = $(CROSS_COMPILE)size

CFLAGS  = -mcpu=cortex-m4 -mthumb -std=c11 -Wall -Wextra -Os
CFLAGS += -ffunction-sections -fdata-sections -DSTM32F4
LDFLAGS = -Wl,--gc-sections -nostartfiles -T linker.ld

TARGET  = Receiver_Module
SRCS    = hal.c
OBJS    = hal.o

all: $(TARGET).elf $(TARGET).bin

$(TARGET).elf: $(OBJS)
	$(CC) $(CFLAGS) $(LDFLAGS) -o $@ $^

$(TARGET).bin: $(TARGET).elf
	$(OBJCOPY) -O binary $< $@

%.o: %.c
	$(CC) $(CFLAGS) -c -o $@ $<

clean:
	rm -f $(OBJS) $(TARGET).elf $(TARGET).bin

.PHONY: all clean
